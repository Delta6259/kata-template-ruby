class Pathlist

  def initialize(from:, to:)
    @from = from
    @to = to
  end

  def all_possible_paths(segments:)
    if segments == nil || segments == []
      []
    else
      # calculer

    end
  end
end